# 2026-03-21 当前进度与后续任务交接说明

## 交接背景

当前协作在用户外出前暂停，需要由下一位 AI 伙伴继续接手推进文档治理型里程碑开发。仓库已经切到专用开发分支，且最近两轮里程碑已经完整闭环，因此可以直接从既定流程继续往下做。

## 请下一位协作者先确认的事实

- 仓库路径：`/Users/fridolph/Desktop/github/Fridolph`
- 当前分支：`codex/m15-next`
- 当前工作区应为干净状态
- 最近完成的两个子任务提交：
  - `7a74e20` `[☲☷][明夷] docs(m15): 完成 H5开发总结子目录治理闭环`
  - `75d6854` `[☲☷][明夷] docs(m16): 完成 H5直播流学习子目录治理闭环`
- 公开分支基线：
  - `master` 指向 `37e7991`
  - 公开分支不再暴露 Dao 历史

## 当前仓库采用的推进方式

后续请继续严格按这条 Dao 闭环执行，不要跳步骤：

1. 阶段规划
2. 任务拆解
3. 业务流程梳理
4. 详细设计
5. 开发实现
6. 自测
7. 记录沉淀
8. 当前子任务按 Dao Commit 提交

每个新里程碑默认都要补齐四类文档：

- `01-任务拆解`
- `02-详细设计`
- `03-开发记录`
- `04-里程碑开发笔记`

统一位置：

- `docs/dao-milestones/`

可复用脚本：

- `node scripts/create-dao-milestone.mjs <milestone> <topic>`

## 当前质量基线与自测要求

当前质量基线文件：

- `scripts/docs-quality-baseline.json`

当前基线值：

- `placeholderCount = 14`
- `missingTitleCount = 151`
- `missingAssetCount = 0`
- `duplicateTitleCount = 24`

每轮治理结束后，至少执行：

1. 对应目录的 `docs:fix-h1:*`
2. `npm run docs:sync`
3. `npm run docs:check`
4. 必要时补跑 `node scripts/docs-check.mjs`

原则：

- 自测不通过就继续修，直到通过为止
- 如果基线下降，应同步更新 `scripts/docs-quality-baseline.json`
- 如果指标回升，不能提交，必须先修复

## 建议的下一里程碑

默认优先继续做 `M17`。

推荐目标：

- `01HTML5/前端路由`

推荐原因：

- 当前该目录仍有 `2` 篇文档缺少一级标题
- 与 `M15`、`M16` 同属 `01HTML5` 主线，连续性强
- 风险低，目录边界清晰，不会碰到 `05Nodejs`

建议命名：

- 里程碑主题：`前端路由子目录治理深化`
- 预计脚本入口：`docs:fix-h1:h5-routing`

建议标题收口方向：

- `hash路由.md` -> `前端路由：Hash 路由`
- `history路由.md` -> `前端路由：History 路由`

## M17 推荐执行顺序

1. 读取 `docs-site/.vitepress/generated/quality-report.json` 确认目标仍成立
2. 生成 `M17` 文档骨架
3. 更新 `docs/dao-milestones/00-阶段里程碑规划.md`
4. 在 `package.json` 中补充新的 `docs:fix-h1:*` 入口
5. 对目标目录执行 H1 修复
6. 做语义化收口，不要只停留在文件名标题
7. 执行自测链路直到通过
8. 回写开发记录、里程碑笔记、质量基线
9. 以 Dao Commit 完成当前子任务提交

## 需要避免的事项

- 不要贸然进入 `05Nodejs` 做大范围治理
- 不要修改 `scripts/fix-missing-h1.mjs` 的通用逻辑，除非用户明确要求
- 不要把多个相邻目录合并成一个大里程碑，当前策略是“小范围、可验证、可提交”
- 不要跳过文档沉淀和自测

## 若用户回来后的自然汇报方式

可以直接告诉用户：

- `M15` 与 `M16` 已完成
- 当前质量基线已下降到 `missingTitleCount = 151`
- 当前建议继续推进 `M17：01HTML5/前端路由`
- 如需阶段推送或后续 squash，可在积累若干子任务后再统一处理
